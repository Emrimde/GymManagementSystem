<UserControl x:Class="GymManagementSystem.WPF.Views.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.Auth"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
    </UserControl.InputBindings>
    <StackPanel Orientation="Vertical" VerticalAlignment="Center">

        <TextBlock
            FontSize="40" 
            FontWeight="Bold" 
            HorizontalAlignment="Center" 
            Margin="0,20,0,0"
            Foreground="White"       
                   >
           <Run Text="Welcome in"/>
           <Run Text="{DynamicResource GymName}"/>
           <Run Text="System"/>
        </TextBlock>

        <TextBlock Grid.Row="1" Text="Login" 
            FontSize="20" 
            FontWeight="Bold" 
            HorizontalAlignment="Center" 
            Margin="0,20,0,20" Foreground="White" />

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Orientation="Vertical" VerticalAlignment="Center"
                >

            <!-- Username -->
            <StackPanel Orientation="Horizontal">
                <Label Content="Username:" 
                VerticalAlignment="Center" 
                Margin="0,0,10,0" Width="100" FontSize="25" Foreground="White" />

                <TextBox Width="200" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                  Height="25" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"  />
            </StackPanel>

            <!-- Password -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Label FontSize="25" Content="Password:" VerticalAlignment="Center" Margin="0,0,10,0" Width="100" Foreground="White" />
                <TextBox Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" x:Name="PwdBox" Width="200" Height="25" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />
            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Login"
        Width="100"
        HorizontalAlignment="Center"
        Margin="0,20,0,0"
        Command="{Binding LoginCommand}" 
        />
                <Button Command="{Binding OpenRegisterViewCommand}" Grid.Column="1" Width="100" Content="Register" Margin="0,20,0,0"></Button>
            </Grid>
        </StackPanel>
    </StackPanel>
</UserControl>
