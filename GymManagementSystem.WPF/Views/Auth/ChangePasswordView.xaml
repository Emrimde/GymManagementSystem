<UserControl x:Class="GymManagementSystem.WPF.Views.Auth.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" Background="{StaticResource BackgoundColor}">

    <Grid>
        <materialDesign:Card Padding="32"
              MaxWidth="400"
              HorizontalAlignment="Center"
              VerticalAlignment="Center">

            <StackPanel>

                <TextBlock Text="Set new password"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Margin="0,0,0,16" />

                <PasswordBox materialDesign:HintAssist.Hint="New password"
                             Margin="0,0,0,12"
                             PasswordChanged="NewPasswordChanged"/>

                <PasswordBox materialDesign:HintAssist.Hint="Confirm password"
                             Margin="0,0,0,24"
                             PasswordChanged="ConfirmPasswordChanged"/>

                <Button Content="Change password"
                        Command="{Binding ChangePasswordCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Height="40"/>

            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
