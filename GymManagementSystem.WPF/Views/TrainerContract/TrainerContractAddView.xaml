<UserControl x:Class="GymManagementSystem.WPF.Views.TrainerContract.TrainerContractAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.TrainerContract"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#2B2B2B">
            <ContentControl Content="{Binding SidebarView}"/>
        </Border>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <TextBlock Margin="0,20,0,0" HorizontalAlignment="Center" FontSize="30" Foreground="{StaticResource SecondColor}" Text="Add new trainer" />
            <Grid Grid.Row="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
             
                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="Contract type " Foreground="White"/>
                        <ComboBox ItemsSource="{Binding ContractTypes}" SelectedItem="{Binding SelectedContractType}"/>
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="Trainer type " Foreground="White"/>
                        <ComboBox ItemsSource="{Binding TrainerTypes}" SelectedItem="{Binding SelectedTrainerType}"/>
                    </StackPanel>


                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal" Visibility="{Binding IsB2B, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock FontSize="25" Text="Company Name " Foreground="White"/>
                        <TextBox Text="{Binding TrainerContract.CompanyName}" VerticalContentAlignment="Center" FontSize="20" Margin="10,0,0,0"  Width="150" />
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal" Visibility="{Binding IsB2B, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock FontSize="25" Text="NIP " Foreground="White"/>
                        <TextBox Text="{Binding TrainerContract.TaxId}" VerticalContentAlignment="Center" FontSize="20" Margin="10,0,0,0"  Width="150" />
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal" Visibility="{Binding IsB2B, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock FontSize="25" Text="Company Address" Foreground="White"/>
                        <TextBox Text="{Binding TrainerContract.CompanyAddress}" VerticalContentAlignment="Center" FontSize="20" Margin="10,0,0,0"  Width="150"/>
                    </StackPanel>
                    
                    
                    
                    
                    
                    
                    
                    
                    
                    
                    
                    
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="ValidFrom " Foreground="White"/>
                        <DatePicker SelectedDate="{Binding TrainerContract.ValidFrom}" SelectedDateFormat="Short" />
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal" >
                        <TextBlock FontSize="25" Text="Valid To " Foreground="White"/>
                        <DatePicker SelectedDate="{Binding TrainerContract.ValidTo}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
            <StackPanel Grid.Row="2" VerticalAlignment="Center" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Add trainer" Command="{Binding AddTrainerCommand }"/>
                <Button Content="Cancel" Command="{Binding ReturnToStaffViewCommand }"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
