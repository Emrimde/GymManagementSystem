<UserControl x:Class="GymManagementSystem.WPF.Views.TrainerContract.TrainerContractDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.TrainerContract"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" Content="{Binding SidebarView}"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Foreground="{DynamicResource PrimaryColor}">
        <Run Text="{Binding TrainerContract.FirstName}"/>
       
        <Run Text="{Binding TrainerContract.LastName}"/>
        <Run Text=": details"/>
            </TextBlock>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Email: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20">
      <Run Text="{Binding TrainerContract.Email}"/>
                        </TextBlock>

                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
 
                        <TextBlock Text="Phone number: " VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                        <TextBlock Text="{Binding TrainerContract.PhoneNumber}" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Club Commission: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20">
      <Run Text="{Binding TrainerContract.ClubCommissionPercent}"/>

                        </TextBlock>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Is signed: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20" Text="{Binding TrainerContract.IsSigned}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Visibility="{Binding  TrainerContract.IsB2B, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Company address: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20">
              <Run Text="{Binding TrainerContract.CompanyAddress}"/>

                        </TextBlock>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Visibility="{Binding  TrainerContract.IsB2B, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Company name: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20" Text="{Binding TrainerContract.CompanyName}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Nip: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"  Visibility="{Binding  TrainerContract.IsB2B, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20" Text="{Binding TrainerContract.TaxId}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Vertical" Grid.Column="1" HorizontalAlignment="Center"  >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Valid from/to: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20">
                            <Run Text="{Binding TrainerContract.Valid}" />
                        </TextBlock>
                    </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Is signed: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                            <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20" Text="{Binding TrainerContract.IsSigned}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Contract type: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                            <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20" Text="{Binding TrainerContract.ContractType}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Trainer type: "  Foreground="{DynamicResource PrimaryColor}" FontSize="20"/>
                            <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryColor}" FontSize="20" Text="{Binding TrainerContract.TrainerType}" />
                        </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" >
                        <Button Content="Show schedule " Width="150"  Command="{Binding OpenTrainerScheduleCommand}" CommandParameter="{Binding TrainerContract.Id}" Visibility="{Binding TrainerContract.CanShowBooking , Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Show Rates " Width="150"  Command="{Binding OpenTrainerRatesCommand}" CommandParameter="{Binding TrainerContract.Id}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Terminate" Width="150" Visibility="{Binding TrainerContract.CanTerminate, Converter={StaticResource BoolToVisibilityConverter}}" Command="{Binding GenerateTerminationCommand}" CommandParameter="{Binding TrainerContract.Id}"/>
                    </StackPanel>

                </StackPanel>


                



            </Grid>
          
        </Grid>

    </Grid>
</UserControl>
