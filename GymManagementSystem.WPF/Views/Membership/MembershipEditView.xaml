<UserControl x:Class="GymManagementSystem.WPF.Views.Membership.MembershipEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.Membership"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#2B2B2B">
            <ContentControl Content="{Binding SidebarView}"/>
        </Border>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <TextBlock Margin="0,50,0,0" HorizontalAlignment="Center" FontSize="30" Foreground="{StaticResource SecondColor}" Text="Edit membership" />

            <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center">

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock FontSize="25" Text="Name" Foreground="White"/>
                    <TextBox Width="400" Margin="10,0,0,0" VerticalContentAlignment="Center"
                 FontSize="20"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock FontSize="20" Text="Booking classes ahead(days in advance)" Foreground="White"/>
                    <TextBox Width="100" Margin="10,0,0,0" VerticalContentAlignment="Center"
                 Text="{Binding ClassBookingDaysInAdvanceCount,
                                UpdateSourceTrigger=PropertyChanged,
                                ValidatesOnNotifyDataErrors=True}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock FontSize="20" Text="Free friend entries / month" Foreground="White"/>
                    <TextBox Width="100" Margin="10,0,0,0" VerticalContentAlignment="Center"
                 Text="{Binding FreeFriendEntryCountPerMonth,
                                UpdateSourceTrigger=PropertyChanged,
                                ValidatesOnNotifyDataErrors=True}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock FontSize="20" Text="Free personal training sessions" Foreground="White" />
                    <TextBox Width="100" Margin="10,0,0,0" VerticalContentAlignment="Center"
                 Text="{Binding FreePersonalTrainingSessions,
                                UpdateSourceTrigger=PropertyChanged,
                                ValidatesOnNotifyDataErrors=True}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Margin="0,0,0,30" Grid.Row="2" VerticalAlignment="Center" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button  Content="Edit membership" Command="{Binding UpdateMembershipCommand }"/>
                <Button Command="{Binding CancelCommand}" Content="Cancel"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
