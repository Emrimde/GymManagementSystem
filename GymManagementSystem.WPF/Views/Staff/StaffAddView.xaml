<UserControl x:Class="GymManagementSystem.WPF.Views.Staff.StaffAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.Staff"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#2B2B2B">
            <ContentControl Content="{Binding SidebarView}"/>
        </Border>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <TextBlock Margin="0,20,0,0" HorizontalAlignment="Center" FontSize="30" Foreground="{StaticResource SecondColor}" Text="Add new person" />
            <Grid Grid.Row="1" >
               
                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="First name" Foreground="White"/>
                        <TextBox Text="{Binding PersonAdd.FirstName}" VerticalContentAlignment="Center" FontSize="20" Margin="10,0,0,0" Width="150"/>
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="Last name" Foreground="White"/>
                        <TextBox Text="{Binding PersonAdd.LastName}" VerticalContentAlignment="Center" FontSize="20" Margin="10,0,0,0"  Width="150"/>
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="Email" Foreground="White"/>
                        <TextBox Text="{Binding PersonAdd.Email}" VerticalContentAlignment="Center" FontSize="20" Margin="47,0,0,0"  Width="150"/>
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="Phone number" Foreground="White"/>
                        <TextBox Text="{Binding PersonAdd.PhoneNumber}" VerticalContentAlignment="Center" FontSize="20" Margin="47,0,0,0"  Width="150"/>
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="City" Foreground="White"/>
                        <TextBox Text="{Binding PersonAdd.City}" VerticalContentAlignment="Center" FontSize="20" Margin="47,0,0,0"  Width="150"/>
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="Street" Foreground="White"/>
                        <TextBox Text="{Binding PersonAdd.Street}" VerticalContentAlignment="Center" FontSize="20" Margin="47,0,0,0"  Width="150"/>
                    </StackPanel>
                   
                </StackPanel>
            </Grid>
            <Button Grid.Row="2" Content="Add person" Width="140" Height="35" Command="{Binding AddPersonAsyncCommand}" />
        </Grid>
    </Grid>
</UserControl>
