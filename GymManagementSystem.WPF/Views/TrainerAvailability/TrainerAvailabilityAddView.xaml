<UserControl x:Class="GymManagementSystem.WPF.Views.TrainerAvailability.TrainerAvailabilityAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.TrainerAvailability"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" Content="{Binding SidebarView}"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Create new trainer availability" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30" Foreground="{DynamicResource PrimaryColor}"/>
            <StackPanel Grid.Row="1" Orientation="Vertical">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Start Time" HorizontalAlignment="Center" FontSize="30" Foreground="{DynamicResource PrimaryColor}"/>
                    <TextBox Margin="20,0,0,0" Width="300" Text="{Binding TrainerAvailabilityAddRequest.StartTime}" HorizontalAlignment="Center" FontSize="30" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <TextBlock Text="End Time" HorizontalAlignment="Center" FontSize="30" Foreground="{DynamicResource PrimaryColor}"/>
                    <TextBox Margin="20,0,0,0" Width="300" Text="{Binding TrainerAvailabilityAddRequest.EndTime}" HorizontalAlignment="Center" FontSize="30" />
                </StackPanel>
            </StackPanel>
            <Button Grid.Row="2" Width="100" Height="50" Content="Create" Command="{Binding CreateTrainerAvailabilityCommand}" />
        </Grid>
    </Grid>
</UserControl>
