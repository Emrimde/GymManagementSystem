<UserControl x:Class="GymManagementSystem.WPF.Views.Termination.TerminationAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.Termination"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" Content="{Binding SidebarView}"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Create new termination" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30" Foreground="{DynamicResource PrimaryColor}"/>
            <StackPanel Grid.Row="1" Orientation="Vertical">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Reason" HorizontalAlignment="Center" FontSize="30" Foreground="{DynamicResource PrimaryColor}"/>
                    <TextBox Foreground="{StaticResource PrimaryColor}"  Height="120" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="20,0,0,0" Width="600" Text="{Binding TerminationAddRequest.Reason}" HorizontalAlignment="Center" FontSize="30" />
                </StackPanel>
            </StackPanel>
            <Button Grid.Row="2" Width="100" Height="50" Content="Create" Command="{Binding CreateTerminationCommand}" />
        </Grid>
    </Grid>
</UserControl>
