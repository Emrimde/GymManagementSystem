<UserControl x:Class="GymManagementSystem.WPF.Views.MembershipPrice.MembershipPriceAddViewModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.MembershipPrice"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#2B2B2B">
            <ContentControl Content="{Binding SidebarView}"/>
        </Border>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <TextBlock Margin="0,20,0,0" HorizontalAlignment="Center" FontSize="30" Foreground="{StaticResource SecondColor}" Text="Add new price for " />
            <Grid Grid.Row="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>

                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">

                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="Price: " Foreground="White"/>
                        <TextBox VerticalContentAlignment="Center" FontSize="25"  Width="100" Text="{Binding MembershipPriceAdd.Price}"/>
                    </StackPanel>
                    <StackPanel Margin="0,10,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontSize="25" Text="Label price (optional):" Foreground="White"/>
                        <TextBox Text="{Binding MembershipPriceAdd.LabelPrice}" VerticalContentAlignment="Center" FontSize="20" Margin="47,0,0,0"  Width="150"/>
                    </StackPanel>
                </StackPanel>

            </Grid>
            <StackPanel Grid.Row="2" VerticalAlignment="Center" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Add membership price" Command="{Binding AddMembershipPriceCommand }"/>
                <Button Content="Cancel" Command="{Binding ReturnToMembershipDetailsCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
