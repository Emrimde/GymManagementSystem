<UserControl x:Class="GymManagementSystem.WPF.Views.ClassBooking.AddClassBookingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.ClassBooking"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="#2F3038">
            <ContentControl Content="{Binding SidebarView}"/>
        </Border>

        <ScrollViewer Grid.Column="1"
                      VerticalScrollBarVisibility="Auto">

            <StackPanel Margin="32">

                <TextBlock FontSize="28"
                           FontWeight="Bold"
                           Margin="0,0,0,24"
                           Foreground="{DynamicResource PrimaryColor}">
                    <Run Text="Book class for "/>
                    <Run Text="{Binding Client.FullName}"/>
                </TextBlock>

                <materialDesign:Card Padding="32"
                                     UniformCornerRadius="16"
                                     materialDesign:ElevationAssist.Elevation="Dp4">

                    <StackPanel>

                        <TextBlock Text="Gym class"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"
                                   Foreground="{DynamicResource PrimaryColor}"/>

                        <ComboBox ItemsSource="{Binding GymClasses}"
                                  DisplayMemberPath="Name"
                                  Foreground="{StaticResource PrimaryColor}"
                                  SelectedItem="{Binding SelectedGymClass}"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  Margin="0,0,0,24"/>

                        <TextBlock Text="Scheduled class"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"
                                   Foreground="{DynamicResource PrimaryColor}"
                                   Visibility="{Binding IsScheduledClassComboBoxVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <ComboBox ItemsSource="{Binding ScheduledClasses}"
                                  DisplayMemberPath="ScheduledClassDetails"
                                  SelectedItem="{Binding SelectedScheduledClass}"
                                  Foreground="{StaticResource PrimaryColor}"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  Margin="0,0,0,32"
                                  Visibility="{Binding IsScheduledClassComboBoxVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right">

                            <Button Content="Cancel"
                                    Margin="0,0,16,0"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Command="{Binding CancelCommand}"/>

                            <Button Content="Book class"
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    Command="{Binding AddClassBookingCommand}"/>

                        </StackPanel>

                    </StackPanel>

                </materialDesign:Card>

            </StackPanel>

        </ScrollViewer>
    </Grid>
</UserControl>
