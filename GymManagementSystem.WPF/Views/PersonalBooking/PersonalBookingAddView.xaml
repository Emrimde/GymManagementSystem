<UserControl x:Class="GymManagementSystem.WPF.Views.PersonalBooking.PersonalBookingAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.WPF.Views.PersonalBooking"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource BackgoundColor}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#2B2B2B">
            <ContentControl Content="{Binding SidebarView}"/>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="26"
                       Foreground="{StaticResource SecondColor}"
                       Text="Add new personal training" />

            <StackPanel Grid.Row="1"
                        Margin="40">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="260"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                        
                    </Grid.RowDefinitions>

                    <TextBlock Text="Date:"  Foreground="{DynamicResource PrimaryColor}"
                               Grid.Row="0"
                               Grid.Column="0"
                               VerticalAlignment="Center"/>

                    <DatePicker Grid.Row="0"
                                Grid.Column="1"
                                SelectedDate="{Binding SelectedDate, Mode=TwoWay}"
                                Width="200"/>

                    <TextBlock Text="Start time:"  Foreground="{DynamicResource PrimaryColor}"
                               Grid.Row="2"
                               Grid.Column="0"
                               VerticalAlignment="Center"/>

                    <ComboBox Grid.Row="2" 
                              Grid.Column="1"
                              Width="200"
                              ItemsSource="{Binding TimeSlots}"
                              SelectedItem="{Binding SelectedStartSlot, Mode=TwoWay}"/>

                    <TextBlock Text="Personal trainer:" Foreground="{DynamicResource PrimaryColor}"
                               Grid.Row="4"
                               Grid.Column="0"
                               VerticalAlignment="Center"/>

                    <ComboBox Grid.Row="4"
                              Grid.Column="1"
                              Width="200"
                              ItemsSource="{Binding PersonalTrainers}"
                              SelectedItem="{Binding SelectedPersonalTrainer, Mode=TwoWay}"
                              DisplayMemberPath="FullName"/>
                    
                    <TextBlock Text="Choose training duration" Foreground="{DynamicResource PrimaryColor}"
                               Grid.Row="6"
                               Grid.Column="0"
                               VerticalAlignment="Center"/>

                    <ComboBox Grid.Row="6"
                              Grid.Column="1"
                              Width="200"
                              ItemsSource="{Binding TrainerRates}"
                              SelectedItem="{Binding SelectedTrainerRate, Mode=TwoWay}"
                              DisplayMemberPath="DisplayPriceDuration"/>
                </Grid>

                <Button Content="Save"
                        Width="140"
                        Margin="0,25,0,0"
                        HorizontalAlignment="Left"
                        Command="{Binding AddPersonalTrainingCommand}"/>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
