<Window x:Class="GymManagementSystem.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GymManagementSystem.Desktop.Views"
        mc:Ignorable="d"
        Background="{StaticResource BackgoundColor}"
        Title="LoginWindow" Height="450" Width="800">
    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
    </Window.InputBindings>
    <StackPanel Orientation="Vertical" VerticalAlignment="Center">

        <TextBlock Text="Welcome to NextLevelGym System" 
             FontSize="40" 
             FontWeight="Bold" 
             HorizontalAlignment="Center" 
             Margin="0,20,0,0"/>

        <TextBlock Grid.Row="1" Text="Login" 
             FontSize="20" 
             FontWeight="Bold" 
             HorizontalAlignment="Center" 
             Margin="0,20,0,20"/>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Orientation="Vertical" VerticalAlignment="Center"
                 >

            <!-- Username -->
            <StackPanel Orientation="Horizontal">
                <Label Content="Username:" 
                 VerticalAlignment="Center" 
                 Margin="0,0,10,0" Width="100" FontSize="25"/>

                <TextBox Width="200" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                   Height="25" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>

            <!-- Password -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Label FontSize="25" Content="Password:" VerticalAlignment="Center" Margin="0,0,10,0" Width="100"/>
                <TextBox Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" x:Name="PwdBox" Width="200" Height="25" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Login"
         Width="100"
         HorizontalAlignment="Center"
         Margin="0,20,0,0"
         Command="{Binding LoginCommand}" 
         />
                <Button Command="{Binding OpenRegisterView}" Grid.Column="1" Width="100" Content="Register" Margin="0,20,0,0"></Button>
            </Grid>
        </StackPanel>
    </StackPanel>
</Window>
